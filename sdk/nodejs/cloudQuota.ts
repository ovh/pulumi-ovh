// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages the public cloud project quota: the applied quota profile per region
 * and the `preventAutomaticQuotaUpgrade` toggle. There is exactly one quota
 * envelope per project, so this
 * resource is a singleton — the project's service name is used as the import id.
 *
 * Regions omitted from `regions` are left unchanged upstream.
 *
 * ## Import
 *
 * The quota envelope is a singleton per project — import by service name only:
 *
 * bash
 *
 * ```sh
 * $ pulumi import ovh:index/cloudQuota:CloudQuota this <service_name>
 * ```
 */
export class CloudQuota extends pulumi.CustomResource {
    /**
     * Get an existing CloudQuota resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CloudQuotaState, opts?: pulumi.CustomResourceOptions): CloudQuota {
        return new CloudQuota(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'ovh:index/cloudQuota:CloudQuota';

    /**
     * Returns true if the given object is an instance of CloudQuota.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CloudQuota {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CloudQuota.__pulumiType;
    }

    /**
     * — Computed hash of the current target specification, used for
     * optimistic concurrency on updates.
     */
    public /*out*/ readonly checksum!: pulumi.Output<string>;
    /**
     * , `updatedAt` — Envelope timestamps (RFC3339).
     */
    public /*out*/ readonly createdAt!: pulumi.Output<string>;
    /**
     * — Live state of the quota:
     */
    public /*out*/ readonly currentState!: pulumi.Output<outputs.CloudQuotaCurrentState>;
    /**
     * — When true, automatic
     * quota upgrades are disabled for this project.
     */
    public readonly preventAutomaticQuotaUpgrade!: pulumi.Output<boolean>;
    /**
     * — Target quota profile per region:
     */
    public readonly regions!: pulumi.Output<outputs.CloudQuotaRegion[]>;
    /**
     * — Quota readiness in the system (`CREATING`, `UPDATING`,
     * `DELETING`, `OUT_OF_SYNC`, `READY`, `ERROR`, `SUSPENDED`, `UNKNOWN`).
     */
    public /*out*/ readonly resourceStatus!: pulumi.Output<string>;
    /**
     * — Service name of the cloud project.
     */
    public readonly serviceName!: pulumi.Output<string>;
    /**
     * Last update date (RFC3339).
     */
    public /*out*/ readonly updatedAt!: pulumi.Output<string>;

    /**
     * Create a CloudQuota resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CloudQuotaArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CloudQuotaArgs | CloudQuotaState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CloudQuotaState | undefined;
            resourceInputs["checksum"] = state ? state.checksum : undefined;
            resourceInputs["createdAt"] = state ? state.createdAt : undefined;
            resourceInputs["currentState"] = state ? state.currentState : undefined;
            resourceInputs["preventAutomaticQuotaUpgrade"] = state ? state.preventAutomaticQuotaUpgrade : undefined;
            resourceInputs["regions"] = state ? state.regions : undefined;
            resourceInputs["resourceStatus"] = state ? state.resourceStatus : undefined;
            resourceInputs["serviceName"] = state ? state.serviceName : undefined;
            resourceInputs["updatedAt"] = state ? state.updatedAt : undefined;
        } else {
            const args = argsOrState as CloudQuotaArgs | undefined;
            if ((!args || args.preventAutomaticQuotaUpgrade === undefined) && !opts.urn) {
                throw new Error("Missing required property 'preventAutomaticQuotaUpgrade'");
            }
            if ((!args || args.regions === undefined) && !opts.urn) {
                throw new Error("Missing required property 'regions'");
            }
            if ((!args || args.serviceName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'serviceName'");
            }
            resourceInputs["preventAutomaticQuotaUpgrade"] = args ? args.preventAutomaticQuotaUpgrade : undefined;
            resourceInputs["regions"] = args ? args.regions : undefined;
            resourceInputs["serviceName"] = args ? args.serviceName : undefined;
            resourceInputs["checksum"] = undefined /*out*/;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["currentState"] = undefined /*out*/;
            resourceInputs["resourceStatus"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CloudQuota.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CloudQuota resources.
 */
export interface CloudQuotaState {
    /**
     * — Computed hash of the current target specification, used for
     * optimistic concurrency on updates.
     */
    checksum?: pulumi.Input<string>;
    /**
     * , `updatedAt` — Envelope timestamps (RFC3339).
     */
    createdAt?: pulumi.Input<string>;
    /**
     * — Live state of the quota:
     */
    currentState?: pulumi.Input<inputs.CloudQuotaCurrentState>;
    /**
     * — When true, automatic
     * quota upgrades are disabled for this project.
     */
    preventAutomaticQuotaUpgrade?: pulumi.Input<boolean>;
    /**
     * — Target quota profile per region:
     */
    regions?: pulumi.Input<pulumi.Input<inputs.CloudQuotaRegion>[]>;
    /**
     * — Quota readiness in the system (`CREATING`, `UPDATING`,
     * `DELETING`, `OUT_OF_SYNC`, `READY`, `ERROR`, `SUSPENDED`, `UNKNOWN`).
     */
    resourceStatus?: pulumi.Input<string>;
    /**
     * — Service name of the cloud project.
     */
    serviceName?: pulumi.Input<string>;
    /**
     * Last update date (RFC3339).
     */
    updatedAt?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a CloudQuota resource.
 */
export interface CloudQuotaArgs {
    /**
     * — When true, automatic
     * quota upgrades are disabled for this project.
     */
    preventAutomaticQuotaUpgrade: pulumi.Input<boolean>;
    /**
     * — Target quota profile per region:
     */
    regions: pulumi.Input<pulumi.Input<inputs.CloudQuotaRegion>[]>;
    /**
     * — Service name of the cloud project.
     */
    serviceName: pulumi.Input<string>;
}
