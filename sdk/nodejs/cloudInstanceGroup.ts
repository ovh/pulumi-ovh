// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Creates an instance group (placement group) in a public cloud project.
 *
 * > **WARNING** An instance group is **immutable**: it has no update route. Changing any attribute (`name`, `region`, `policy`) forces the group to be destroyed and re-created.
 *
 * > **NOTE** Membership is set only at instance-creation time through the instance's `groupId`; this resource never manages members.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ovh from "@ovhcloud/pulumi-ovh";
 *
 * const group = new ovh.CloudInstanceGroup("group", {
 *     serviceName: "<Public cloud project id>",
 *     region: "GRA11",
 *     name: "my-instance-group",
 *     policy: "ANTI_AFFINITY",
 * });
 * ```
 *
 * ### AFFINITY vs ANTI_AFFINITY
 *
 * `ANTI_AFFINITY` spreads the members across distinct hypervisors: use it for the
 * replicas of a highly-available service, so a single host failure cannot take
 * them all down. `AFFINITY` does the opposite and packs the members onto the same
 * hypervisor, for tightly coupled instances that benefit from staying on one host.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ovh from "@ovhcloud/pulumi-ovh";
 *
 * const spread = new ovh.CloudInstanceGroup("spread", {
 *     serviceName: "<Public cloud project id>",
 *     region: "GRA11",
 *     name: "my-anti-affinity-group",
 *     policy: "ANTI_AFFINITY",
 * });
 * const packed = new ovh.CloudInstanceGroup("packed", {
 *     serviceName: "<Public cloud project id>",
 *     region: "GRA11",
 *     name: "my-affinity-group",
 *     policy: "AFFINITY",
 * });
 * ```
 *
 * ## Import
 *
 * An instance group in a public cloud project can be imported using the
 *
 * `service_name` and `instance_group_id`, separated by `/`:
 *
 * terraform
 *
 * import {
 *
 *   to = ovh_cloud_instance_group.group
 *
 *   id = "<service_name>/<instance_group_id>"
 *
 * }
 *
 * bash
 *
 * ```sh
 * $ pulumi import ovh:index/cloudInstanceGroup:CloudInstanceGroup group service_name/instance_group_id
 * ```
 */
export class CloudInstanceGroup extends pulumi.CustomResource {
    /**
     * Get an existing CloudInstanceGroup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CloudInstanceGroupState, opts?: pulumi.CustomResourceOptions): CloudInstanceGroup {
        return new CloudInstanceGroup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'ovh:index/cloudInstanceGroup:CloudInstanceGroup';

    /**
     * Returns true if the given object is an instance of CloudInstanceGroup.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CloudInstanceGroup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CloudInstanceGroup.__pulumiType;
    }

    /**
     * Computed hash representing the current target specification value. Because an instance group has no update route, this value never changes after creation.
     */
    public /*out*/ readonly checksum!: pulumi.Output<string>;
    /**
     * Creation date of the instance group.
     */
    public /*out*/ readonly createdAt!: pulumi.Output<string>;
    /**
     * Current state of the instance group:
     */
    public /*out*/ readonly currentState!: pulumi.Output<outputs.CloudInstanceGroupCurrentState>;
    /**
     * Instance group name. **Changing this value recreates the resource.**
     */
    public readonly name!: pulumi.Output<string>;
    /**
     * Placement policy applied to the group's member instances (`AFFINITY`, `ANTI_AFFINITY`). **Changing this value recreates the resource.**
     */
    public readonly policy!: pulumi.Output<string>;
    /**
     * Region where the instance group will be created. **Changing this value recreates the resource.**
     */
    public readonly region!: pulumi.Output<string>;
    /**
     * Instance group readiness in the system (`CREATING`, `DELETING`, `ERROR`, `OUT_OF_SYNC`, `READY`)
     */
    public /*out*/ readonly resourceStatus!: pulumi.Output<string>;
    /**
     * Service name of the resource representing the id of the cloud project. **Changing this value recreates the resource.**
     */
    public readonly serviceName!: pulumi.Output<string>;

    /**
     * Create a CloudInstanceGroup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CloudInstanceGroupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CloudInstanceGroupArgs | CloudInstanceGroupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CloudInstanceGroupState | undefined;
            resourceInputs["checksum"] = state ? state.checksum : undefined;
            resourceInputs["createdAt"] = state ? state.createdAt : undefined;
            resourceInputs["currentState"] = state ? state.currentState : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
            resourceInputs["policy"] = state ? state.policy : undefined;
            resourceInputs["region"] = state ? state.region : undefined;
            resourceInputs["resourceStatus"] = state ? state.resourceStatus : undefined;
            resourceInputs["serviceName"] = state ? state.serviceName : undefined;
        } else {
            const args = argsOrState as CloudInstanceGroupArgs | undefined;
            if ((!args || args.policy === undefined) && !opts.urn) {
                throw new Error("Missing required property 'policy'");
            }
            if ((!args || args.region === undefined) && !opts.urn) {
                throw new Error("Missing required property 'region'");
            }
            if ((!args || args.serviceName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'serviceName'");
            }
            resourceInputs["name"] = args ? args.name : undefined;
            resourceInputs["policy"] = args ? args.policy : undefined;
            resourceInputs["region"] = args ? args.region : undefined;
            resourceInputs["serviceName"] = args ? args.serviceName : undefined;
            resourceInputs["checksum"] = undefined /*out*/;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["currentState"] = undefined /*out*/;
            resourceInputs["resourceStatus"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CloudInstanceGroup.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CloudInstanceGroup resources.
 */
export interface CloudInstanceGroupState {
    /**
     * Computed hash representing the current target specification value. Because an instance group has no update route, this value never changes after creation.
     */
    checksum?: pulumi.Input<string>;
    /**
     * Creation date of the instance group.
     */
    createdAt?: pulumi.Input<string>;
    /**
     * Current state of the instance group:
     */
    currentState?: pulumi.Input<inputs.CloudInstanceGroupCurrentState>;
    /**
     * Instance group name. **Changing this value recreates the resource.**
     */
    name?: pulumi.Input<string>;
    /**
     * Placement policy applied to the group's member instances (`AFFINITY`, `ANTI_AFFINITY`). **Changing this value recreates the resource.**
     */
    policy?: pulumi.Input<string>;
    /**
     * Region where the instance group will be created. **Changing this value recreates the resource.**
     */
    region?: pulumi.Input<string>;
    /**
     * Instance group readiness in the system (`CREATING`, `DELETING`, `ERROR`, `OUT_OF_SYNC`, `READY`)
     */
    resourceStatus?: pulumi.Input<string>;
    /**
     * Service name of the resource representing the id of the cloud project. **Changing this value recreates the resource.**
     */
    serviceName?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a CloudInstanceGroup resource.
 */
export interface CloudInstanceGroupArgs {
    /**
     * Instance group name. **Changing this value recreates the resource.**
     */
    name?: pulumi.Input<string>;
    /**
     * Placement policy applied to the group's member instances (`AFFINITY`, `ANTI_AFFINITY`). **Changing this value recreates the resource.**
     */
    policy: pulumi.Input<string>;
    /**
     * Region where the instance group will be created. **Changing this value recreates the resource.**
     */
    region: pulumi.Input<string>;
    /**
     * Service name of the resource representing the id of the cloud project. **Changing this value recreates the resource.**
     */
    serviceName: pulumi.Input<string>;
}
